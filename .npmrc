# Disable pnpm 11+ supply-chain minimum-release-age policy, which blocks
# packages published within the last ~24h (e.g. browserslist data updates)
# and fails CI builds with ERR_PNPM_MINIMUM_RELEASE_AGE_VIOLATION.
minimum-release-age=0
